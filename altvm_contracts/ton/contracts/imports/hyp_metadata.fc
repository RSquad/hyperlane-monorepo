slice metadata::refund_addr(slice metadata) inline {
  metadata~skip_bits(16 + 256 + 256);
  return metadata~load_msg_addr();
}

int metadata::unpack::gas_limit(slice metadata) inline {
    metadata~skip_bits(16 + 256);
    return metadata~load_uint(256);
}